# Implementation Plans — Index

**Status: S1 landed (P00–P04, P28 part 1). S2 landed (P05, P07, P08). 28 plans remain.**
**All decisions are closed. Ready to execute.**

**Next session: S3** (P06 → P09 → P10 → P11), branch `fix/data-layer` off
`fix/request-safety`. See [`../branch-info.md`](../branch-info.md).

37 plans. One plan = one fix = one commit. Numbered in execution order;
dependencies are stated per plan. Written as instructions to myself, not as
documentation to read.

---

## 🔴 Confirmed live exposure — 2026-08-07

Fetched <https://github-userexplorer.netlify.app/> and its bundle
`/assets/index-CnEJiIFO.js` (571 KB). It contains **4 occurrences of a
`ghp_`-prefixed GitHub token**. The value was deliberately not recorded.

**B1 (revoke that token) is still outstanding.** It does not block any plan.

---

## Repo state at hand-off (verified 2026-08-07)

| Fact | Consequence |
|---|---|
| `.env.development` exists at repo root, defines `GITHUB_TOKEN`, **gitignored and untracked** ✅ | No token has ever been committed — confirmed across all branches |
| ~~⚠️ Its key has a **trailing space**: `GITHUB_TOKEN =`~~ ✅ **fixed in P28** (`07e7274`) | The value was also quoted *and* whitespace-padded. Both stripped; still untracked |
| ⚠️ ~~Client code still reads `VITE_GITHUB_AUTHENTICATION_TOKEN` in 7 hooks~~ → **after S2: exactly 1 read and 1 `Authorization` header, both in `helper/githubFetch.ts`** | Removing the variable's *definition* did not change the *code*. The site is still broken (`Bearer undefined`) until **P34**. Accepted — zero traffic. *(The "7" was S1's miscount — it was 8 before S2, `useFetchLoginType` was missed. **P34: re-grep, do not trust either number.**)* |
| ~~`guide.txt`, `package.json`, `package-lock.json` deleted on disk (unstaged)~~ | Committed in `27c9555`, before S1. `guide.txt` recoverable from history → **P36**. The root manifest deletion resolves V10 §10b |
| `extra/`, `README.md` still present | **P36**, **P33** |
| `.github/dependabot.yml` already present at `27c9555` | **P32** appears to have landed early, outside its session. Verify before re-doing it in S10 |

---

## Decisions recorded — all closed

| | Decision | Effect |
|---|---|---|
| **B2** | **Keep the contribution graph** → backend proxy | GraphQL requires auth, so token-free was never viable. **P34** |
| **B3** | **Netlify**, <https://github-userexplorer.netlify.app/> | Netlify Functions + one `netlify.toml`. **P34**, **P35** |
| **B4** | **Delete `useFetchSearchUsers`** | Dead duplicate; search itself untouched. Folded into **P02** |
| **B5** | **Build the navbar search** — delegated to me, decided yes | Shared `<SearchBar>`, submit-only, no autocomplete. **P37** |
| **B6** | **MIT licence** | **P36** |
| **B7** | **Keep `report/` private** — never linked from the README | Constrains **P33** |
| **B8** | **Move `guide.txt` + `extra/` to `docs/`** | `git mv`; `guide.txt` restored from `HEAD` first. **P36** |
| **B9** | **Use Zod** — for env parsing *and* API responses | Env schema **P28**; response schemas **P09**. Fonts: keep Google Fonts for now |

---

## Environment (verified 2026-08-07)

| Capability | Status |
|---|---|
| npm registry | ✅ reachable (`npm ping` → PONG) — `npm ci`, lint, tsc, build all runnable |
| `client/node_modules` | ✅ installed by **P00** (230 pkgs, no lockfile drift). Baseline was **green** — see [`_baseline.txt`](_baseline.txt) |
| Node / npm | v24.18.1 / 12.0.2 |
| Python PIL | ✅ 12.1.1 — favicon + image resizing possible |
| ImageMagick / cwebp / optipng | ❌ missing — use PIL instead |
| Git | ✅ clean tree, branch `rework/2026` |

**Universal verification gate.** Every plan ends with:

```bash
cd client && npm run lint && npx tsc -b --noEmit && npm run build
```

If any of the three fails, the plan is not done. No plan is complete on "it
looks right".

---

## ✅ Can implement without you (33)

Landed plans carry ✅ and their commit SHA. P00–P04 and P28 are on
`fix/quick-wins`; P05, P07 and P08 are on `fix/request-safety`.

| # | Plan | Fixes | Depends on | Risk |
|---|---|---|---|---|
| [P00](P00.baseline-and-branch.md) ✅ `d32addb` | Baseline: install, verify green, branch | *(prerequisite)* | — | none |
| [P01](P01.rel-noopener.md) ✅ `928fad0` | `rel="noopener noreferrer"` on 3 links | V08 | P00 | none |
| [P02](P02.dead-code-removal.md) ✅ `f828f7a` | Delete commented-out and unreachable code | S12b/d | P00 | low |
| [P03](P03.skeleton-keyframes.md) ✅ `4cfac47` | Skeleton animation that never runs | S03c | P00 | none |
| [P04](P04.login-validation-helper.md) ✅ `9183b81` | `isValidLogin` / `isValidRepoName` helper | V02/V03 groundwork | P00 | none |
| [P05](P05.github-fetch-helper.md) ✅ `05f757e` | `githubFetch` + typed error classes | V05a, V07b, V09 | P04 | med |
| [P06](P06.query-key-registry.md) | **Cache-key collision bugs** | S02 | P00 | low |
| [P07](P07.url-encoding.md) ✅ `b22c5dc` | Encode all interpolated URL input | V03 | P04, P05 | med |
| [P08](P08.graphql-variables.md) ✅ `7dc2c03` | GraphQL variables, not interpolation | V02 | P05, P07 | med |
| [P09](P09.typed-api-responses.md) | Generics at the fetch boundary | S04 | P05, P08 | med |
| [P10](P10.dedupe-user-fetch.md) | Delete duplicate `/users/:login` hook | S02c, V07d | P06, P09 | low |
| [P11](P11.query-client-defaults.md) | Retry policy, staleTime, no refetch-on-focus | V07c/d | P05, P10 | low |
| [P12](P12.error-boundary.md) | Root error boundary — stops white screens | V05 | P00 | low |
| [P13](P13.error-state-component.md) | `<ErrorState>` replaces 6 raw error renders | V09 | P05, P12 | low |
| [P14](P14.profileinfo-guard-order.md) | **Blank profile page bug** | S03a | P13 | low |
| [P15](P15.pagination-nan.md) | **`Math.ceil(undefined/8)` → NaN bug** | S03b | P09, P10 | low |
| [P16](P16.notfound-and-empty-states.md) | Real 404, empty states, kill `"demoUserName"` | S10 | P04, P14 | med |
| [P17](P17.storage-validation.md) | Validate localStorage at the boundary | V06 | P04 | low |
| [P18](P18.starred-context-rearchitecture.md) | Hoist provider, lazy init, state-based reads | S06a-e | P17 | med |
| [P19](P19.starred-rename.md) | `stared`→`starred` rename, pure refactor | S12e | P18 | low |
| [P20](P20.search-form-fixes.md) | Kill `alert()`, fix the trim bug | S05a/b/e | P04 | low |
| [P21](P21.infinite-scroll-guard.md) | Observer fires unthrottled | V07a | P11 | low |
| [P22](P22.aria-labels-and-landmarks.md) | Icon-button names, alt text, `<main>` | S09a/d/g/h | P00 | low |
| [P23](P23.pagination-as-links.md) | Pagination buttons → real links | S09b | P15, P22 | low |
| [P24](P24.stat-cards-semantics.md) | Disabled buttons → non-interactive cards | S09c | P22 | low |
| [P25](P25.contribution-grid-a11y.md) | Grid semantics + contrast | S09e/f | P22 | med |
| [P26](P26.image-assets.md) | 1.4 MB favicon, 500 KB logos, unused files | S07a/b | P00 | med |
| [P27](P27.route-code-splitting.md) | Lazy routes + Suspense | S07c | P12, P16 | low |
| [P28](P28.env-example-and-typing.md) ◑ `07e7274` | **Zod-validated env config** + `.env.example` | V10a | P00 | none |
| [P29](P29.eslint-hardening.md) | `jsx-a11y` + `jsx-no-target-blank` | V08, S09 | P01, P22-P25 | low |
| [P30](P30.vitest-and-unit-tests.md) | Vitest + tests for the extracted pure logic | S11 | P04, P15 | low |
| [P31](P31.github-actions-ci.md) | CI: lint + tsc + test + build | S11 | P30 | low |
| [P32](P32.dependabot.md) | Dependabot config | V10d | P00 | none |
| [P33](P33.readme-draft.md) | README draft → `docs/README.draft.md` (non-destructive) | S08 | P00 | none |
| [P34](P34.netlify-functions-proxy.md) | **Backend proxy — fixes the critical token exposure** | **V01**, S01 | P05, P07-P09 | high |
| [P35](P35.netlify-config-headers.md) | `netlify.toml`: SPA fallback, CSP, headers, caching | V04, S13 | P34, P26 | med |
| [P36](P36.docs-relocation-and-license.md) | `docs/` relocation + MIT LICENSE | S12f, S08 | P00 | low |
| [P37](P37.navbar-search.md) | Shared `<SearchBar>` + navbar search *(B5: building it)* | S05 (feature) | P16, P20 | med |

◑ = partially landed. **P28** shipped steps 1-3, 5, 6 in S1; **step 4** (the
server env schema at `netlify/functions/_shared/env.mts`) is deferred to **S4**,
where P34 first creates that directory. `zod@^4.4.3` is installed and available
to **P09**.

**Recommended batches** (each independently shippable):

- **A — Quick wins:** P00 → P01 → P02 → P03 (~40 min, zero risk) — ✅ **done**, plus P04 and P28 part 1 (session S1)
- **B — Hook layer:** P04 → P05 → P06 → P07 → P08 → P09 → P10 → P11 *(one pass; doing these separately means refactoring the same 8 files five times)*
- **B2 — 🔴 Proxy + deploy config:** P34 → P35 *(**the critical fix**; strictly after batch B, which is what makes P34 a URL swap in one file)*
- **C — Error & state UI:** P12 → P13 → P14 → P15 → P16
- **D — Storage/context:** P17 → P18 → P19
- **E — Search & scroll:** P20 → P21 → P37
- **F — Accessibility:** P22 → P23 → P24 → P25
- **G — Assets & perf:** P26 → P27
- **H — Tooling:** P28 → P29 → P30 → P31 → P32 → P33 → P36

**Priority is now B → B2.** Batch B fixes the two highest-severity items
reachable without you (V02, V03) and both confirmed cache bugs; batch B2 then
closes the critical one. P05 deliberately funnels every token read into a single
file so that P34 becomes a URL swap rather than a rewrite — that is why the
order matters.

⚠️ **P35 must not ship before P34.** Its `connect-src 'self'` breaks a client
that still calls `api.github.com` directly.

---

## ⛔ Still outstanding (1) — an action, not a decision

| # | Item | What you do |
|---|---|---|
| **B1** | 🔴 **Revoke the exposed PAT** — confirmed live in the bundle | Only you can. Sequence so the site never breaks in between: create a new **no-scope** token → add it as `GITHUB_TOKEN` in **Netlify → Site settings → Environment variables** → ship **P34** → *then* revoke the old one at <https://github.com/settings/tokens>. Also check <https://github.com/settings/security-log>. |

**B1 blocks nothing.** All 37 plans can be written and shipped without it; the
deploy simply needs the env var present before **P34** goes live.

### Two follow-ups I deliberately deferred (not blockers)

- **Self-hosted fonts** (`@fontsource/roboto`) — would let two CSP exceptions be dropped in **P35** and remove a third-party round trip. Keeping Google Fonts for now; revisit after **P26**/**P27**.
- **MSW + Playwright** — **P30** covers pure-function tests only. Hook and end-to-end tests are a second session.

---

## Rules for myself while executing

1. **One plan, one commit.** Commit message references the plan: `fix(security): encode user input in GitHub API URLs (P07)`. Never bundle plans.
2. **Run the verification gate before every commit.** Lint + tsc + build, all three green. No exceptions.
3. **Never touch anything on the blocked list.** If a plan turns out to need a blocked decision, stop, leave the tree clean, and record it — do not guess.
4. **Do not commit to `main`.** Work on `rework/2026`, or a `fix/` branch off it.
5. **Do not delete a file whose only copy is in the working tree** — `git mv` to `docs/` instead. Applies to the image masters in P26.
6. **Do not add a runtime dependency** without it being named in the plan. Pre-approved: `zod` (P28/P09, runtime), and the dev dependencies in P29/P30. Nothing else.
7. **Do not change visible behaviour in a plan labelled "pure refactor"** (P19). If the diff shows a behaviour change, it's wrong.
8. **Do not write a test that asserts current buggy behaviour.** P30 runs after P15 for exactly this reason.
9. **Do not "improve" adjacent code.** Scope creep across 33 plans compounds into an unreviewable diff. Note it, move on.
10. **Report honestly.** If a plan half-lands, say which half. If the build breaks, say so with the output.
11. **Never print an env value** — in logs, error messages, or commit messages. Field names only.

---

## 📋 Session & branch batching → [`../branch-info.md`](../branch-info.md)

37 plans grouped into **11 sessions**, batched by *files touched* so each session
reads a set of files into context once. Start there, not here, when opening a new
chat. Critical path to the token fix: **S1 → S2 → S3 → S4**.

Every plan file carries a **stale-information warning** at the top: its line
numbers and code excerpts are from the audit snapshot (`1fa3e0f`, 2026-08-07),
and earlier plans edit the same files. Intent and acceptance criteria are
authoritative; coordinates are not.

### What S1 changed that later plans must account for

- ❌ ~~**There are now 7 token-reading hooks, not 8.**~~ **Wrong — it was 8**, corrected by S2 below. P02 deleted `useFetchSearchUsers.ts` (B4), but `useFetchLoginType` was missed in the recount. The advice the note gave was right even though its number was not: any plan counting hooks or `Authorization` headers — **P34** — must **re-grep** rather than trust a figure written down in a previous session.
- **`UserProfileRepos` no longer takes a `username` prop.** Dropping the dead `navigate()` state object orphaned it and `noUnusedLocals` failed the build, so the prop and its `DisplayRepoList` call site went too. **P07 §5** rebuilds the path from `owner`/`name` on the repo object, so it does not need the prop back.
- **`Explorer.tsx` now renders one end-of-results message.** The second was nested inside `{hasNextPage && …}` and could never render. Relevant to **P16** (empty states) and **P21** (scroll guard).
- **`LoadingSkeleton` uses MUI `<Skeleton>`.** No hand-rolled cell `<Box>`, no `#e0e0e0`. Relevant to **P25**.
- **`.gitignore` gained `!.env.example`** after the `.env*` rules. Do not reorder those lines — negations only work after the rule they undo.

### What S2 changed that later plans must account for

- **Every GitHub request goes through `client/src/helper/githubFetch.ts`** —
  `githubFetch<T>(url)` and `githubGraphQL<T>(query, variables)`. There are no
  `fetch(` calls left in `client/src/hooks`, and exactly one `import.meta.env`
  read in the whole of `client/src`. **P34** is now a change to that one file
  plus moving `constants/graphqlQueries.ts` server-side.
- **The index undercounted: 8 hooks read the token before S2, not 7.** P02
  deleted `useFetchSearchUsers`, but `useFetchLoginType` was missed in the
  count. All 8 are migrated.
- **`githubGraphQL` returns `data`, not the envelope.** `OrganizationTop10ReposType`
  and `ContributionCalendarResponse` in `common.types.ts` lost their outer
  `data:` wrapper, and `LoginTypeResponse` is new. Relevant to **P09**.
- **Typed errors exist:** `GitHubError` / `NotFoundError` / `RateLimitError` in
  `helper/githubErrors.ts`. **P11**'s retry predicate and **P13**'s renderer
  should `instanceof` these rather than string-match messages. `RateLimitError`
  carries `resetAt`.
- ⚠️ **`erasableSyntaxOnly` is on in `tsconfig.app.json`** — constructor
  parameter properties do not compile. P05's plan sketched them; the classes use
  declared fields plus assignment instead. Any new class must do the same.
- **URLs are built in `helper/githubUrls.ts`** (`usersUrl`, `userReposUrl`,
  `repoUrl`, `searchUsersUrl`). **P34** reroutes these four functions, not the
  hooks.
- **`helper/parsePage.ts` exists** and `Repositories.tsx` / `DisplayRepoList.tsx`
  use it, so no `NaN` reaches a request URL. **P15** still owns `Pagination`'s
  own `Math.ceil(totalRepos / 8)` arithmetic — that is untouched. **P30** has a
  pure function to test.
- ⚠️ **Two type assertions were left as deliberate markers** —
  `Repositories.tsx` (`as number` on `public_repos`) and `DisplayRepoList.tsx`
  (`as Repo[]`). They exist only because P05 made optionality visible that `any`
  was hiding. **P09/P15 must delete them**, not preserve them.
- **`useFetchContributionInfo` and `useFetchOrganizationRepos` still share the
  `["contributionInfo", username]` query key.** Left alone on purpose — **P06**.
- **`UserProfileRepos` now navigates to `/user/:owner/:repoName`** with both
  segments encoded, instead of relying on `full_name`'s raw `/`. **P23** should
  keep that shape when it converts to links.
- **`LowerHomeUI` navigates via `createSearchParams`.** Its `alert()` and the
  `searchTerm.length` vs trimmed-length bug are untouched — **P20**.

---

## Working across chat sessions

You intend to run one chat instance per feature. Each plan file is written to be
self-contained for exactly that: a fresh session needs only its plan plus this
index. To hand off cleanly:

- **Start each session by naming the plan** ("implement P07"), and point it at this index for the dependency graph and the rules above.
- **Respect the dependency column.** Batch B (P04→P11) in particular is one continuous pass over the same 8 hook files; splitting it across sessions means merge conflicts with yourself.
- **Update this index when a plan lands** — add a ✅ and the commit SHA to its row, so the next session knows what is already true.
- **P00 runs once**, in the first session only.
