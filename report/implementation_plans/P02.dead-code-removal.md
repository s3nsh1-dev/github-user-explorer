# P02 · Delete commented-out and unreachable code

**Fixes:** [suggestions/12](../suggestions/12.repo-hygiene-and-dead-code.md) §12b, §12d, and [suggestions/10](../suggestions/10.empty-and-notfound-states.md) §10e
**Depends on:** P00 · **Blocks:** — (but do early; every later plan touches less)
**Autonomy:** full · **Risk:** low · **Est:** 20 min

**Session:** S1 · **Branch:** `fix/quick-wins`

> ### ⚠️ Read before editing any file
>
> **Every line number and code excerpt below describes the repo as of the audit
> snapshot — commit `1fa3e0f`, 2026-08-07.** Plans that run earlier in the
> sequence edit these same files, so by the time this one runs the coordinates
> may have moved and quoted code may already look different.
>
> - **Re-read the target file before editing it.** Never patch by line number.
> - **The plan's *intent*, *acceptance criteria*, and *Do NOT* list are authoritative. Its line numbers and code snippets are not** — they are a map, not the territory.
> - Check the **Depends on** line above for which plans have already landed.
> - If what you find differs materially from what is described here, that is expected. Adapt, and **say so in the completion report** rather than forcing the plan's shape onto changed code.
> - If a referenced bug appears already fixed, do not "re-fix" it. Verify, note it, move on.

## Why

Doing this first means the refactors in batch B edit smaller files, and the
diffs stay readable. Pure deletion — git is the history.

## Steps

### 1. `client/src/page/Explorer.tsx`
- Delete the commented-out "Load More" `<Button>` block (~lines 97-104).
- Delete the now-orphaned `style4` (~line 21) and the commented `Button` import (~line 4).
- **Delete the unreachable `{!hasNextPage && …}` block nested inside `{hasNextPage && …}`** (~lines 110-114). It can never render. Keep the outer one at ~118.

### 2. `client/src/hooks/useFetchUserData.ts`
- Delete the commented `cacheTime` block (~lines 31-35). *(The v4→v5 `gcTime` rename it documents gets handled properly in **P11**.)*

### 3. `client/src/components/UserCards.tsx` + `common.types.ts`
- `seeRepos` is declared in `UserCardsProps` and passed from `Explorer.tsx:85` but never destructured or used. Remove the prop from the type, the JSX call site, and nothing else.

### 4. `client/src/components/UserProfileRepos.tsx` (~lines 45-50)
- `navigate(\`/user/${repo.full_name}\`, { state: { repoName, username } })` — `ShowSelectedRepo` reads `useParams()`, never `location.state`. Drop the state object.
- **Leave `repo.full_name` in the path for now.** Building it from `owner`/`name` explicitly is part of **P07**.

### 5. Small commented-out style lines
- `muiCustomTheme.ts` ~9-14 (primary/secondary palette), `OrganizationTopRepos.tsx` ~53 (`// width: "50%"`), `ProfileInfo.tsx` ~39 (`// wordBreak`), plus any similar one-liners in `PageQuickButtons` / `UpperHomeUI`.

### 6. Vite template leftovers
- `git rm client/src/assets/react.svg client/public/vite.svg` — unambiguously template junk, not referenced anywhere.

### 7. Delete `useFetchSearchUsers.ts` — **B4 answered: delete**

Verified unused: `grep -rn "useFetchSearchUsers" client/src` returns only its
own definition and export lines. `Explorer` uses `useInfiniteUsers`; nothing
imports this one.

```bash
git rm client/src/hooks/useFetchSearchUsers.ts
```

Re-run the grep after deleting — must return nothing. **Search behaviour is
completely unaffected**; this hook has never been wired to anything.

## Acceptance criteria

- [ ] `grep -rn "// <Button\|// style4\|cacheTime" client/src` → no hits
- [ ] `grep -rn "seeRepos" client/src` → no hits
- [ ] Explorer renders one end-of-results message, not two
- [ ] Verification gate green
- [ ] `noUnusedLocals` still passes (it will catch anything I orphaned)

## Do NOT

- **Do not touch `useFetchReposPerPage.ts:34`** (the commented-out `staleTime`). That is a live behaviour bug, fixed deliberately in **P11**, not silently here.
- **Do not touch `guide.txt` or `extra/`** here — **P36** relocates them (B8 answered).
- **Do not touch `useInfiniteUsers.ts`.** That is the live search. Only the unused duplicate goes.
- **Do not delete the image masters** (`*_original.png`, `github-logo.png`). **P26** relocates them; they may be the only copies.
- Do not rename `style1`…`style6`. Cosmetic, high churn, zero value right now.
